{
  "name": "tampermonkey-scripts",
  "version": "0.3.16",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node scripts/validate.mjs",
    "validate": "npm run build && npm test",
    "release:patch": "npm version patch --no-git-tag-version && npm run validate",
    "release:x-tweaks": "node scripts/release-x-tweaks.mjs --profile Default",
    "chrome:check-installed": "node scripts/check-installed-version.mjs",
    "chrome:trigger-update": "node scripts/trigger-chrome-update.mjs"
  },
  "dependencies": {
    "jsdom": "^26.1.0"
  }
}
